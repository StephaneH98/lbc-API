version: 1
frontend:
  phases:
    # Pas besoin de build pour du HTML/CSS/JS statique
    build:
      commands:
        - echo "Déploiement du frontend statique"
        - |
          cat > env.js << EOF
          window.ENV = {
            API_URL: '${API_URL}',
            BUCKET_NAME: '${BUCKET_NAME}',
            REGION: '${REGION}'
          };
          EOF
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
